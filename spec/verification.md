# Cryptographic Verification

Cryptographic verification specifies integrity by proving that received bytes match published identities.

Equation:

```tex
V = \left(H(C_{received}) = H(C_{published})\right)
```
