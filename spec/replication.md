# Distributed Replication

Distributed replication specifies availability by preserving independent copies across independent operators and paths.

Core statement:

> The network—not any individual server—is the infrastructure.

Equation:

```tex
A \propto RP
```
